[project]
name = "dbt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dbt-bigquery>=1.12.0",
]

[dependency-groups]
dev = [
    "sqlfluff>=4.3.0",
    "sqlfluff-templater-dbt>=4.3.0",
]
